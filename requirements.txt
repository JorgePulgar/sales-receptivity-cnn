# Core ML stack (TF 2.10 = last version with native GPU support on Windows)
tensorflow==2.10.1
protobuf==3.19.6

# Data science
numpy==1.23.5
pandas==2.0.3
scikit-learn==1.3.2
scipy==1.10.1

# Visualization
matplotlib==3.7.5
seaborn==0.13.2
plotly==5.18.0

# Computer vision
opencv-python==4.9.0.80
Pillow==10.2.0
# mediapipe is intentionally excluded — phase 4 uses OpenCV Haar Cascades.
# Only add it back if Haar Cascades prove insufficient.

# Grad-CAM
tf-keras-vis==0.8.6

# API
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
pydantic==2.6.1

# Demo
streamlit==1.18.1
altair==4.2.2  # streamlit 1.18 imports altair.vegalite.v4; altair >=5 dropped that path

# Jupyter
jupyter==1.0.0
notebook==7.0.8
ipykernel==6.29.2

# Dataset download
kaggle==1.6.6

# Testing
pytest==8.0.1
httpx==0.26.0

# TF.js export (phase 10)
# tensorflowjs 4.x requires TF >= 2.12; 3.21.0 is the last release that
# accepts TF 2.10.x without upgrading the core stack.
tensorflowjs==3.21.0

# Utilities
tqdm==4.66.2
python-dotenv==1.0.1
