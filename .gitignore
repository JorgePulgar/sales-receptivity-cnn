/data/
# Trained model weights — do NOT commit to git.
# Exception: docs/models/ is intentionally tracked (TF.js weights for GitHub Pages).
# The rules below match only the top-level models/ directory, not docs/models/.
models/*.keras
models/*.h5
.ipynb_checkpoints/
__pycache__/
*.pyc
venv/
.env
.kaggle/
.claude/
demo/sample_videos/
examples/
